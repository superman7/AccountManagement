<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.digitalchina.xa.it.dao.TopicOptionDAO" >
  <sql id="BASE_TABLE">
    t_topicoption
  </sql>

  <sql id="BASE_COLUMN">
    id,optionkey,topicid,popularity
  </sql>

	<insert id="insert" parameterType="com.digitalchina.xa.it.model.TopicOptionDomain">
		INSERT INTO
			<include refid="BASE_TABLE"/>
		<trim prefix="(" suffix=")" suffixOverrides=",">
			optionkey,topicid,
		</trim>
		<trim prefix="VALUES(" suffix=")" suffixOverrides=",">
			#{optionkey, jdbcType=VARCHAR},#{topicid, jdbcType=INTEGER}
		</trim>
	</insert>
	
	<select id="selectTopicOptionByTopicID" parameterType="com.digitalchina.xa.it.model.TopicOptionDomain" resultType="com.digitalchina.xa.it.model.TopicOptionDomain">
	    SELECT
	      <include refid="BASE_COLUMN"/>
	    FROM
	      <include refid="BASE_TABLE"/>
	    where topicid = #{topicid, jdbcType=INTEGER}
	</select>
	
	<update id="updatePopularity" parameterType="com.digitalchina.xa.it.model.TopicOptionDomain">
	   UPDATE 
	   <include refid="BASE_TABLE"/>
	   SET    popularity = popularity+1
	   WHERE  id = #{id, jdbcType=INTEGER}
	</update>
</mapper>